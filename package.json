{
  "name": "@nkticket/common",
  "version": "1.0.4",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del-cli build",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"made update to package using script\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "del-cli": "^6.0.0",
    "typescript": "^5.6.3"
  }
}
